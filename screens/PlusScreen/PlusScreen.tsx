import styles from './styles';

import { Text, View } from '../../components/Themed';
import { RootTabScreenProps } from '../../types';

export default function PlusScreen({ navigation }: RootTabScreenProps<'Plus'>) {
  return (
    <View style={styles.container}>
    </View>
  );
}

